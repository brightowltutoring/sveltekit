// export const prerender = true;
export const prerender = false; //  TODO: FORM ACTIONS

// still unsure whether to SSG defined globally (prerender = true), or comment out and have default SSR

// note: only physics route has 'prerender = false'
