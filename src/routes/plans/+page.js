// import { get } from "svelte/store";
// import { isDarkMode } from "$lib/store.js";

// export async function load({ params }) {
//   return {
//     someMessage: get(isDarkMode),
//   };
// }

// when passed into svelte component this value is console logged; it is reactively console logged on the client side only, not server side
